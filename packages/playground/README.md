# @gausszhou/mock-pty-playground

A playground/demo app for `@gausszhou/mock-pty` using Vue 3 + xterm.js.

## Usage

```bash
pnpm install
pnpm dev
```

Opens at `http://localhost:5173/` with a full-screen xterm.js terminal connected to a mock PTY.
